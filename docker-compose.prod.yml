# Image is built in GitHub Actions and pushed to GHCR. Deploy sets ROOMRENTAL_API_IMAGE (see workflow).
services:
  api:
    image: ${ROOMRENTAL_API_IMAGE}
    container_name: roomrental-api
    restart: unless-stopped
    # Bind to loopback only — nginx on the host proxies TLS; avoids exposing the API past the reverse proxy.
    ports:
      - "127.0.0.1:5000:5000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  app-network:
    driver: bridge
